@using EntityLayer.Concrete
@model List<AnasayfaAltMenu>

<div class="col-md-4 mt-4 mt-md-0">
    <h4 class="text-primary">@Html.Raw(@ViewBag.menuad)</h4>
    <ul class="nav nav-list flex-column">
        @foreach (var item in Model)
        {
            <li class="nav-item"><a class="nav-link" href="@item.AltMenuUrl">@item.AltMenuIsim</a></li>
        }
    </ul>
</div>