@using EntityLayer.Concrete
@model List<isilanlari>
            <div class="col">

                        @foreach (var i in Model)
                        {



                            <div class="col-sm-6">
                                <div class="testimonial testimonial-style-4">
                                    <div class="m-2">
                                        <h4 class="m-2 border-bottom-light">@Html.Raw(@i.Baslik)</h4>
                                        <p class="m-2">@Html.Raw(@i.Aciklama)</p>
                                    </div>
                                    <input class="btn btn-primary btnopen" data-aciklama="@Html.Raw(@i.Aciklama)" data-baslik="@Html.Raw(@i.Baslik)" type="button" Value="Görüntüle" />
                                </div>
                            </div>

                          


                             <div id="myModal" class="modal " role="dialog">
                            <div class="modal-dialog">

                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h4 class="mb-2 modal-title"></h4>
                                        <button type="button" class="close" data-dismiss="modal">&times;</button>

                                    </div>
                                    <div class="modal-body">
                                        <p class="m-0 modal-acilklama"></p>
                                    </div>
                                    <div class="modal-footer">
                                        <a href="/IK/BasvuruYap/@i.Id" @*type="submit"*@ class="btn btn-primary" @*Value="Başvur"*@>başvur</a>
@*                                        <input type="submit" class="btn btn-primary btnClose" Value="Kapat" />
*@
                                    </div>
                                </div>

                            </div>
                        </div>
                        }

                       

                    </div>
